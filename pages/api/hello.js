// Next.js API route support: https://nextjs.org/docs/api-routes/introduction

export default function handler(req, res) {
  res.status(200).json({ name: 'John Salchichon' })
}

// 1. {tema, contenido}
// 2. CRUD
// 3. Persisitencia mongodb, mongoAtlas (nuve)